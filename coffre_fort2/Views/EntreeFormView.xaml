<Window x:Class="coffre_fort2.Views.EntreeFormView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:coffre_fort2.Converters"
        Title="Entree" Height="380" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Window.Resources>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Vertical">
            <TextBlock Text="Nom"/>
            <TextBox x:Name="NomBox" Height="30" Margin="0 0 0 10" Text="{Binding Entree.NomApplication, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Vertical">
            <TextBlock Text="Identifiant"/>
            <TextBox x:Name="IdentifiantBox" Height="30" Margin="0 0 0 10" Text="{Binding Entree.Identifiant, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Vertical">
            <TextBlock Text="Mot de passe"/>
            <TextBox x:Name="MotDePasseBox" Height="30" Margin="0 0 0 10" Text="{Binding Entree.MotDePasse, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <CheckBox x:Name="ActiverTagsBox" Content="Activer les tags" Grid.Row="3" Margin="0 0 0 10"
                  IsChecked="{Binding TagsActifs}"/>

        <StackPanel Grid.Row="4" Orientation="Vertical"
                    Visibility="{Binding TagsActifs, Converter={StaticResource BoolToVisibilityConverter}}">
            <TextBlock Text="Tags (separes par des virgules)"/>
            <TextBox x:Name="TagsBox" Height="30" Margin="0 0 0 10" Text="{Binding TagsTexte}"/>
        </StackPanel>

        <Button Grid.Row="5" Content="Valider" Width="100" HorizontalAlignment="Right" Click="Valider_Click"/>
    </Grid>
</Window>
