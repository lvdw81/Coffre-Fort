<Window x:Class="coffre_fort2.Views.ChoixFormatView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Choix du format"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Height="180" Width="300">

	<StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
		<TextBlock Text="Quel format souhaitez-vous utiliser ?" Margin="10" FontWeight="Bold" TextAlignment="Center"/>
		<Button Content="Exporter en CSV" Click="ExporterCsv_Click" Margin="10"/>
        <Button Content="Exporter en XML" Click="ExporterXml_Click" Margin="10"/>
    </StackPanel>
</Window>
